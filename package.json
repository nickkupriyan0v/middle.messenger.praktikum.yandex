{
  "name": "middle.messenger.praktikum.yandex",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "tsc --noEmit",
    "build": "npm run prebuild && vite build",
    "start": "npm run prebuild && node server.cjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.89.2",
    "typescript": "^5.8.3",
    "vite": "^7.0.4",
    "vite-plugin-handlebars": "^2.0.0"
  },
  "dependencies": {
    "express": "^5.1.0",
    "handlebars": "^4.7.8"
  }
}
